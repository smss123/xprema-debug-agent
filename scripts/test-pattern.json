{
  "layer": "ABP",
  "error_signal": "cannot resolve service for type 'IEmailSender'",
  "title": "ABP DI: IEmailSender not registered after module upgrade",
  "root_cause": "After upgrading ABP module, the IEmailSender was no longer auto-registered because the email module was removed from [DependsOn] during migration cleanup",
  "fix_template": "Add the email module back to [DependsOn(...)] and verify DI registration",
  "fix_code": "[DependsOn(\n    typeof(AbpEmailingModule), // FIX: was removed during migration\n    typeof(AbpSettingManagementModule)\n)]\npublic class MyAppModule : AbpModule { }",
  "verification": [
    "dotnet build — no DI exception",
    "Send test email — 200 response",
    "Check DI container: services contains IEmailSender"
  ],
  "tags": ["di", "module", "email", "upgrade", "dependson"],
  "abp_version": "8.x",
  "dotnet_version": "8"
}
